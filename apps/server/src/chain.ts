/**
 * Phase 2–3 (backendspec §11): Helius holder enumeration + Meteora DBC / DAMM v2 claim + payout.
 * Stubs live here until `HELIUS_API_KEY`, `TOKEN_MINT`, pool addresses, and creator keypair are configured.
 */
export const chainPhasePlanned = true;
